<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="rooms.list">
      <field name="name">rooms list</field>
      <field name="model">rooms.rooms</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="rooms.action_window">
      <field name="name">rooms window</field>
      <field name="res_model">rooms.rooms</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="rooms.action_server">
      <field name="name">rooms server</field>
      <field name="model_id" ref="model_rooms_rooms"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="rooms" id="rooms.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="rooms.menu_1" parent="rooms.menu_root"/>
    <menuitem name="Menu 2" id="rooms.menu_2" parent="rooms.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="rooms.menu_1_list" parent="rooms.menu_1"
              action="rooms.action_window"/>
    <menuitem name="Server to list" id="rooms" parent="rooms.menu_2"
              action="rooms.action_server"/>
    -->
  </data>
</odoo>